# HIPAA RAG Frontend Environment Variables
# Copy this file to .env.local and update with your actual values

# Public API Base URL (safe to expose to client-side)
NEXT_PUBLIC_API_BASE=https://hipaa-rag-api.onrender.com

# Server-only API Key (NEVER expose to client - stays on server)
RENDER_API_KEY=your-render-generated-api-key

# Development (optional - for local backend testing)
# NEXT_PUBLIC_API_BASE=http://localhost:8000  
# RENDER_API_KEY=dev-test-key-123

# REQUIRED FOR PRODUCTION:
# Set both variables in Vercel:
# 1. NEXT_PUBLIC_API_BASE (Public) - your Render app URL
# 2. RENDER_API_KEY (Server-only) - your API key from Render